<%= simple_form_for [@fortune, @comment] do |f| %>
  <%= f.input :author, :input_html => {:size => 50} %>
  <%= f.input :body, :input_html => {:rows => 4, :cols => 50} %>
  <%= f.button :submit %>
<% end %>
